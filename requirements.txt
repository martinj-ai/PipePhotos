google-generativeai>=0.8.0
google-genai>=1.0.0
python-dotenv>=1.0.0
Pillow>=10.0.0
Flask>=3.0.0
ImageHash>=4.3.0
higgsfield-client>=0.1.0
imageio-ffmpeg>=0.5.0
playwright>=1.40.0
playwright-stealth>=2.0.0
patchright>=1.59.0  # Playwright undetected (bypass DataDome Expedia)
weasyprint>=60.0    # PDF export (alternative à Playwright PDF)
SQLAlchemy>=2.0     # ORM portable SQLite (local) ↔ Postgres (Railway prod)
psycopg2-binary>=2.9  # Driver Postgres (utilisé en prod via DATABASE_URL)
gunicorn>=21        # WSGI server pour Railway (Flask dev server pas adapté en prod)
